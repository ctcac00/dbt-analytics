semantic_models:
  - name: order_items
    model: ref('order_items')
    entities:
      - name: order_item
        expr: order_item_id
        type: primary
    measures:
      - name: median_revenue
        description: the median revenue generated for each order item
        agg: median
        expr: product_price
